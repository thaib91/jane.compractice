let initialState = {
    cardItem : []
};

const UPDATE_CARDITEM = 'UPDATE_CARDITEM';






export default function reducer(reduxState=initialState, action){
    switch(action.type){
        default: return reduxState;
    }
}